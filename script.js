const menuButton=document.querySelector('.menu-btn');const nav=document.querySelector('nav');menuButton?.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('nav a').forEach(link=>link.addEventListener('click',()=>nav.classList.remove('open')));const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}})},{threshold:.1});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const prices={1:80,2:120,3:200,4:260};let vehicleFee=0;let vehicleType='Sedan / Coupe';const packageSelect=document.querySelector('#packageSelect');
function calculate(){const tier=packageSelect?.value||'1';let total=prices[tier]+vehicleFee;document.querySelectorAll('.addons input:checked').forEach(input=>total+=Number(input.value));['estimate','estimateBottom','buttonPrice'].forEach(id=>{const el=document.getElementById(id);if(el)el.textContent=`$${total}`});return total}
document.querySelectorAll('.package-card').forEach(card=>card.addEventListener('click',()=>{document.querySelectorAll('.package-card').forEach(x=>x.classList.remove('selected'));card.classList.add('selected');packageSelect.value=card.dataset.tier;calculate();document.querySelector('#book').scrollIntoView({behavior:'smooth'})}));
document.querySelectorAll('.vehicle').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.vehicle').forEach(x=>x.classList.remove('selected'));button.classList.add('selected');vehicleFee=Number(button.dataset.fee);vehicleType=button.dataset.vehicle;calculate()}));packageSelect?.addEventListener('change',()=>{document.querySelectorAll('.package-card').forEach(card=>card.classList.toggle('selected',card.dataset.tier===packageSelect.value));calculate()});document.querySelectorAll('.addons input').forEach(input=>input.addEventListener('change',calculate));
document.querySelector('#bookingForm')?.addEventListener('submit',event=>{event.preventDefault();const data=new FormData(event.currentTarget);const addons=[...document.querySelectorAll('.addons input:checked')].map(x=>x.dataset.label).join(', ')||'None';const total=calculate();const message=`Hi Raydonous, I'd like to request a mobile detail.%0A%0AName: ${encodeURIComponent(data.get('name'))}%0APhone: ${encodeURIComponent(data.get('phone'))}%0AEmail: ${encodeURIComponent(data.get('email')||'Not provided')}%0AVehicle: ${encodeURIComponent(data.get('vehicle'))}%0AVehicle type: ${encodeURIComponent(vehicleType)}%0APackage: Tier ${encodeURIComponent(data.get('package'))}%0AAdd-ons: ${encodeURIComponent(addons)}%0AEstimated total: $${total}%0APreferred date: ${encodeURIComponent(data.get('date'))}%0APreferred time: ${encodeURIComponent(data.get('time'))}%0AService address: ${encodeURIComponent(data.get('address'))}%0ANotes: ${encodeURIComponent(data.get('details')||'None')}%0A%0AI understand this is an estimate and Ray will confirm final pricing.`;window.location.href=`sms:+18132939734?&body=${message}`});document.querySelector('#year').textContent=new Date().getFullYear();calculate();