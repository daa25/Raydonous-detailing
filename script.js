const menuButton=document.querySelector('.menu-btn');const nav=document.querySelector('nav');menuButton?.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('nav a').forEach(link=>link.addEventListener('click',()=>nav.classList.remove('open')));const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}})},{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));document.querySelectorAll('[data-package]').forEach(button=>{button.addEventListener('click',()=>{const select=document.querySelector('select[name="package"]');select.value=button.dataset.package;document.querySelector('#book').scrollIntoView({behavior:'smooth'});setTimeout(()=>document.querySelector('input[name="name"]').focus(),500)})});document.querySelector('#bookingForm')?.addEventListener('submit',event=>{event.preventDefault();const data=new FormData(event.currentTarget);const message=`Hi Raydonous, I'd like to request a detail.%0A%0AName: ${encodeURIComponent(data.get('name'))}%0APhone: ${encodeURIComponent(data.get('phone'))}%0AVehicle: ${encodeURIComponent(data.get('vehicle'))}%0AVehicle type: ${encodeURIComponent(data.get('vehicleType'))}%0APackage: ${encodeURIComponent(data.get('package'))}%0APreferred date: ${encodeURIComponent(data.get('date')||'Flexible')}%0ADetails: ${encodeURIComponent(data.get('details')||'No additional notes')}`;window.location.href=`sms:+18132939734?&body=${message}`});document.querySelector('#year').textContent=new Date().getFullYear();